Today, I reviewed floating-point numbers in C++ along with the `<iomanip>` library, and also looked at the `while` loop, since I hadn't fully grasped how it worked—even though I already knew when to use it.
